# Frontend

Simple Windows 95–style desktop built with Vite + React. Shows a Tic Tac Toe window and taskbar UI using React95 components. All game data is stored locally in the browser's local storage.

## Setup
```bash
npm install
```

## Run (dev)
```bash
npm run dev
```
